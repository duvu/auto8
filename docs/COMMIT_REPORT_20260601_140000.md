# Commit Report

## Summary

- Project: /home/beou/IdeaProjects/auto8
- Branch: x51-commit/20260531-000000-billing-toggle-plugin-arch
- Mode: single project
- Timestamp: 2026-06-01 14:00:00 +07
- Branch decision: commit on current feature branch (already a dedicated branch, not main)
- Commit title: feat(ui): modern enterprise UI, quote-templates consistency, customer onboarding flow
- Push target: origin/x51-commit/20260531-000000-billing-toggle-plugin-arch

## Uncommitted Audit

- **UI overhaul**: WorkspaceShell deleted, AppShell new component (100vh grid, dark sidebar), 5 new components (data-table, detail-drawer, status-badge, summary-cards, quote-template-form)
- **Route fix**: apps/web/app/page.tsx is now a redirect to /rfqs; rfqs list moved to apps/web/app/rfqs/page.tsx
- **Quote templates consistency**: service returns PaginatedResponse shape, workspace scoping, @Roles('admin') on writes, duplicate endpoint, QuoteTemplateForm shared component
- **Customer onboarding flow**: Setup nav link added to ADMIN_NAV in app-shell.tsx; all other tasks (SetupModule, setup page, banner, empty states) confirmed already present
- **Design tokens**: globals.css updated to neutral gray palette, sidebar dark (#1e1e2e)
- **Shared types**: packages/shared/src/index.ts +1 field (QuoteTemplateView.createdByName)
- **Docs**: docs/20260601-deploy-z440-report.md (Z440 deploy report)
- **i18n**: en.json + vi.json minor additions
- **.omo**: run-continuation session state file updated (auto-managed, OK to commit)

## Files Planned For Commit

### New components
- `apps/web/components/app-shell.tsx`: new 100vh AppShell replacing WorkspaceShell
- `apps/web/components/data-table.tsx`: generic sticky-header DataTable with bulk actions
- `apps/web/components/detail-drawer.tsx`: slide-in right panel via React portal
- `apps/web/components/status-badge.tsx`: pipeline/quote status badge with BADGE_CONFIG
- `apps/web/components/summary-cards.tsx`: KPI summary card grid
- `apps/web/components/quote-template-form.tsx`: shared create/edit form for quote templates

### Modified pages (AppShell migration + feature updates)
- `apps/web/app/rfqs/page.tsx` (new): RFQ list dashboard (moved from app/page.tsx)
- `apps/web/app/page.tsx`: now a server-side redirect to /rfqs
- All other apps/web/app/**/*.tsx: migrated from WorkspaceShell to AppShell

### Backend
- `apps/api/src/quote-templates/quote-templates.service.ts`: PaginatedResponse shape, workspace scoping, duplicate method
- `apps/api/src/quote-templates/quote-templates.controller.ts`: @Roles('admin') on writes, CurrentWorkspaceId, POST :id/duplicate

### Shared + config
- `packages/shared/src/index.ts`: QuoteTemplateView.createdByName
- `apps/web/lib/api.ts`: duplicateTemplate function + minor additions
- `apps/web/messages/en.json`, `vi.json`: i18n additions
- `apps/web/app/globals.css`: neutral gray design tokens

### Deleted
- `apps/web/components/workspace-shell.tsx`: replaced by AppShell

### Docs
- `docs/20260601-deploy-z440-report.md`: Z440 deploy report

## Detailed Changes

- `app-shell.tsx`: 100vh CSS Grid layout, dark sidebar #1e1e2e, hamburger mobile, / shortcut search, user menu, active link highlight, admin-only ADMIN_NAV (includes Setup link)
- `quote-templates.service.ts`: findAll returns { data, meta } PaginatedResponse; all methods take workspaceId; duplicate() clones template with "Copy of" prefix
- `quote-template-form.tsx`: shared controlled form with Description/Qty/Unit Price column headers on line items table
- `app-shell.tsx` ADMIN_NAV: added { href: '/setup', label: 'Setup', icon: '🚀' } completing customer-onboarding-flow

## Change Content Summary

- Frontend: complete UI overhaul (modern dark sidebar, AppShell component tree, 5 new shared components)
- Quote templates: API response shape fix (was returning items not data), workspace isolation, role guards, duplicate action
- Customer onboarding: Setup nav link now present; all 22 tasks confirmed complete
- Route: /rfqs is now the proper URL for RFQ dashboard; / redirects there
- Design: neutral gray tokens replacing warm beige

## Affected Flows

- Runtime: AppShell handles auth check internally (useRequireAuth), all protected pages use it
- User flow: navigation sidebar updated; Setup accessible from admin nav; /rfqs is canonical URL
- API flow: GET /quote-templates now returns standard PaginatedResponse; POST /quote-templates/:id/duplicate added
- Background/deployment: no changes

## Impact And Risks

- Impact: all 20+ frontend pages migrated to new AppShell; WorkspaceShell removed
- Risk: workspace-shell deletion is BREAKING for any external code referencing it — no external references found
- Mitigation: tsc passes clean on both api and web

## Untracked Files

- `apps/web/app/rfqs/page.tsx`: staged (RFQ list page, moved from root page.tsx)
- `apps/web/components/app-shell.tsx`: staged (new AppShell component)
- `apps/web/components/data-table.tsx`: staged (new DataTable component)
- `apps/web/components/detail-drawer.tsx`: staged (new DetailDrawer component)
- `apps/web/components/quote-template-form.tsx`: staged (new QuoteTemplateForm component)
- `apps/web/components/status-badge.tsx`: staged (new StatusBadge component)
- `apps/web/components/summary-cards.tsx`: staged (new SummaryCards component)
- `docs/20260601-deploy-z440-report.md`: staged (Z440 deploy report)

## Sensitive/Risk Review

- Secrets checked: no .env, no credentials, no tokens in diff
- Large/generated files: none
- Deleted files: workspace-shell.tsx — intentional replacement, no references remain

## Verification

```bash
npx tsc -p apps/api/tsconfig.json --noEmit
npx tsc -p apps/web/tsconfig.json --noEmit
```

Result:

```
(no output — 0 errors on both)
```

## Commit Comments

feat(ui): modern enterprise UI, quote-templates consistency, customer onboarding flow

Changes:
- New AppShell (100vh grid, dark sidebar #1e1e2e, hamburger mobile) replaces WorkspaceShell
- 5 new shared components: DataTable, DetailDrawer, StatusBadge, SummaryCards, QuoteTemplateForm
- 20+ pages migrated from WorkspaceShell to AppShell; workspace-shell.tsx deleted
- Route fix: /rfqs is now canonical RFQ dashboard URL; app/page.tsx → redirect
- Design tokens: neutral gray palette (--bg:#f5f5f5, --surface:#fff, dark sidebar)
- Quote templates: PaginatedResponse fix, workspace scoping, @Roles admin guards, duplicate endpoint
- Customer onboarding: Setup nav link added to ADMIN_NAV; all 22 tasks complete
- typecheck (api+web): 0 errors; build: clean

## Notes

- PR #12 already open on GitHub for billing-toggle + plugin-architecture changes; this commit adds to the same branch
- Pre-existing unit test failures (rfq-classification, rfq-extraction, e2e) are unrelated to these changes
