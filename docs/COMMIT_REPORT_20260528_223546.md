# Commit Report

## Summary

- **Project:** /home/beou/IdeaProjects/auto8
- **Branch:** x51-commit/20260528-223546-catalogue-outlook-simplify
- **Mode:** Single project
- **Timestamp:** 2026-05-28 22:35:46
- **Branch decision:** New branch — large multi-area changeset spanning 5 completed OpenSpec changes: catalogue-management, multi-inbox-management, outlook-connector, mvp1-audit-and-fixes, simplify-codebase. Includes schema migrations, new modules, and frontend pages.
- **Commit title:** feat: catalogue management, Outlook connector, multi-inbox UI, codebase simplification
- **Push target:** origin/x51-commit/20260528-223546-catalogue-outlook-simplify

## Uncommitted Audit

- **Catalogue Management (30/30 tasks):** Product create/full-update/reactivate/CSV-export/dry-run-preview endpoints + frontend pages
- **Multi-Inbox Management (19/19 tasks):** GET /connectors/:id, GET /connectors/:id/runs, POST /connectors/:id/sync; sync history expand panel; connector edit page
- **Outlook Connector (31/31 tasks):** OutlookConnectorService (Graph API), OutlookModule, scheduler cron, bootstrap from env vars, DB dedup, attachment download
- **MVP1 Audit & Fixes (34/34 tasks):** Attachment error-path aggregate fix, double rfq_extract guard, SMTP_SECURE coercion fix, email body discount fix, catalogue case-insensitive search, stale job recovery, extracted customer UI
- **Simplify Codebase (23/23 tasks):** Extracted `optionalString`/`buildPaginatedResponse`/`useAuthUser`/`API_BASE_URL` shared utilities; `calcQuoteTotals` replaces `computeGrandTotal`; `VALID_PIPELINE_STATUSES` moved to shared; Gmail `syncMessages` dedup; Slack `buildNormalizedSlackIntake` dedup; `ConnectorSyncSummary` consolidated to `@auto8/shared`
- **OpenSpec archive:** 5 changes archived to `openspec/changes/archive/2026-05-28-*/`; 6 new/updated main specs written

## Files Planned For Commit

### New files
- `apps/api/src/outlook/outlook-connector.service.ts` — Outlook Graph API connector service
- `apps/api/src/outlook/outlook.module.ts` — OutlookModule
- `apps/api/src/catalogue/dto/create-product.dto.ts` — CreateProductDto with validators
- `apps/api/src/common/utils/paginate.ts` — `buildPaginatedResponse` helper
- `apps/api/src/common/utils/string.util.ts` — `optionalString` helper
- `apps/api/prisma/migrations/20260528095653_add_outlook_message_id/migration.sql` — outlookMessageId on RfqIntake
- `apps/api/prisma/migrations/20260528170000_add_outlook_source_type/migration.sql` — outlook in RfqSourceType enum
- `apps/web/app/catalogue/[id]/page.tsx` — product edit page
- `apps/web/app/catalogue/new/page.tsx` — new product form
- `apps/web/app/connectors/[id]/edit/page.tsx` — connector edit page
- `apps/web/lib/config.ts` — shared API_BASE_URL constant
- `apps/web/lib/use-auth-user.ts` — useAuthUser() React hook

### Modified files
- `apps/api/prisma/schema.prisma` — outlookMessageId String?, RfqSourceType enum adds "outlook"
- `packages/shared/src/index.ts` — 8+ new/modified types; VALID_PIPELINE_STATUSES exported; ConnectorSyncSummary moved here
- `apps/api/src/catalogue/catalogue.service.ts` — create(), fullUpdate(), reactivate(), exportCsv(), dryRunUpload()
- `apps/api/src/catalogue/catalogue.controller.ts` — POST /, PUT /:id, POST /:id/reactivate, GET /export, POST /upload/preview
- `apps/api/src/connector-registry/connector-registry.service.ts` — findOneView(), syncNow(), bootstrapOutlook()
- `apps/api/src/connector-registry/connector-registry.controller.ts` — GET /:id, GET /:id/runs, POST /:id/sync
- `apps/api/src/connector-registry/connector-registry.module.ts` — imports OutlookModule; controller removed from providers
- `apps/api/src/connector-registry/dto/create-connector.dto.ts` — "outlook" added to type enum; @IsNotEmpty on label
- `apps/api/src/connectors/connector.interface.ts` — sync() + testConnector() added to ConnectorService; ConnectorSyncSummary removed (now in shared)
- `apps/api/src/gmail/gmail.service.ts` — syncMessages() extracted; fetchMessages() private; GmailSyncSummary removed
- `apps/api/src/gmail/gmail.module.ts` — ConnectorRunModule added to imports
- `apps/api/src/slack/slack-connector.service.ts` — buildNormalizedSlackIntake() extracted; dedup throw→skip; dead sendConfirmation() removed
- `apps/api/src/slack/slack.module.ts` — ConnectorRunModule added to imports
- `apps/api/src/scheduler/scheduler.service.ts` — runOutlook() cron added; runGmail() uses syncLegacy() vs sync()
- `apps/api/src/scheduler/scheduler.module.ts` — OutlookModule imported
- `apps/api/src/rfqs/rfq-intake.service.ts` — VALID_PIPELINE_STATUSES from shared; outlookMessageId persisted; buildPaginatedResponse used
- `apps/api/src/rfqs/rfq-extraction.service.ts` — pipeline update via updatePipelineStatus
- `apps/api/src/rfqs/rfqs.controller.ts` — RfqPipelineStatus typed body; export-sheet endpoint cleaned
- `apps/api/src/rfqs/quote-workflow.service.ts` — calcQuoteTotals replaces computeGrandTotal; JobsService required (not Optional)
- `apps/api/src/audit/audit.service.ts` — buildPaginatedResponse used
- `apps/api/src/jobs/jobs.controller.ts` — nextRunAt in serialized view
- `apps/api/src/users/users.service.ts` — buildPaginatedResponse used
- `apps/api/src/scheduler/connector-run.service.ts` — ConnectorSyncSummary from shared
- `apps/api/src/scheduler/connector-runs.service.ts` — buildPaginatedResponse + connectorId filter
- `apps/api/src/app.module.ts` — OutlookModule imported
- `apps/web/app/catalogue/page.tsx` — Export CSV, Reactivate, Edit buttons; buildPaginatedResponse shape
- `apps/web/app/catalogue/upload/page.tsx` — 3-stage preview flow
- `apps/web/app/connectors/page.tsx` — Sync now, View history expand panel, Edit link
- `apps/web/app/connectors/new/page.tsx` — Outlook type + credential hints
- `apps/web/app/page.tsx` — useAuthUser hook; VALID_PIPELINE_STATUSES dropdown; formatState directly
- `apps/web/app/rfqs/[rfqId]/page.tsx` — useAuthUser hook
- `apps/web/app/users/page.tsx` — useAuthUser hook
- `apps/web/lib/api.ts` — createProduct, updateProduct, reactivateProduct, exportCatalogue, previewCatalogueUpload, getProduct, getConnector, getConnectorRuns, syncConnectorNow, deactivateUser renamed; ConnectorSyncSummary from shared
- `apps/web/lib/auth.ts` — API_BASE_URL from config.ts
- `package-lock.json` — @microsoft/microsoft-graph-client, isomorphic-fetch, @azure/msal-node, @microsoft/microsoft-graph-types

## Change Content Summary

- **New API endpoints:** POST /catalogue, PUT /catalogue/:id, POST /catalogue/:id/reactivate, GET /catalogue/export, POST /catalogue/upload/preview, GET /connectors/:id, GET /connectors/:id/runs, POST /connectors/:id/sync
- **New frontend pages:** /catalogue/new, /catalogue/[id] (edit), /connectors/[id]/edit; enhanced upload (preview step)
- **New connector:** Outlook/Microsoft Graph (hourly cron, DB-backed, AES-encrypted credentials, attachment download)
- **Code deduplication:** 4 shared utilities extracted; 2 service methods merged; 2 dead types removed
- **Schema migrations:** 2 new migrations (outlookMessageId, RfqSourceType outlook)
- **OpenSpec:** 5 changes archived; 6 main specs synced

## Affected Flows

- **RFQ intake flow:** Outlook connector now ingests via `classifyAndIntake()` on hourly cron
- **Catalogue flow:** Operators can create/edit/reactivate products manually; upload has preview step
- **Connector management flow:** Admins can sync on-demand, view per-connector run history, edit connector
- **Quote workflow:** `calcQuoteTotals` from shared used server-side; JobsService always available

## Impact And Risks

- **Impact:** High — 5 completed changes merged; new Outlook dependency (@azure/msal-node, graph-client)
- **Risk:** Outlook credentials require Azure App Registration setup by deployer; no-op without OUTLOOK_* env vars
- **Mitigation:** All Outlook paths guard on `isConfigured()` / `findAllEnabled()`; no startup failure if unconfigured
- **Schema migrations:** 2 new Prisma migrations — run `prisma migrate deploy` on deployment

## Untracked Files

All 12 untracked files staged:
- `apps/api/prisma/migrations/20260528095653_add_outlook_message_id/migration.sql` — staged
- `apps/api/prisma/migrations/20260528170000_add_outlook_source_type/migration.sql` — staged
- `apps/api/src/catalogue/dto/create-product.dto.ts` — staged
- `apps/api/src/common/utils/paginate.ts` — staged
- `apps/api/src/common/utils/string.util.ts` — staged
- `apps/api/src/outlook/outlook-connector.service.ts` — staged
- `apps/api/src/outlook/outlook.module.ts` — staged
- `apps/web/app/catalogue/[id]/page.tsx` — staged
- `apps/web/app/catalogue/new/page.tsx` — staged
- `apps/web/app/connectors/[id]/edit/page.tsx` — staged
- `apps/web/lib/config.ts` — staged
- `apps/web/lib/use-auth-user.ts` — staged

## Sensitive/Risk Review

- No secrets, tokens, or private keys in staged files
- `package-lock.json` updated — 3 new Microsoft packages added (graph-client, msal-node, types)
- `schema.prisma` modified — migrations generated and applied to local DB
- No `.env` files staged

## Verification

```bash
npm run typecheck && npm run build && npm run test
```

Result:
- typecheck: PASS (0 errors in all 3 workspaces)
- build: PASS (17 Next.js routes generated)
- test: PASS (58/58 tests)

## Commit Comments

feat: catalogue management, Outlook connector, multi-inbox UI, codebase simplification

Changes from 5 completed OpenSpec changes:

catalogue-management (30 tasks):
- POST /catalogue — manual product create (409 on dup)
- PUT /catalogue/:id — full update
- POST /catalogue/:id/reactivate — idempotent reactivation
- GET /catalogue/export — CSV download
- POST /catalogue/upload/preview — dry-run preview returning UploadPreviewResult
- Frontend: /catalogue/new, /catalogue/[id] edit page, 3-stage upload with preview

multi-inbox-management (19 tasks):
- GET /connectors/:id — single connector detail
- GET /connectors/:id/runs — paginated run history per connector
- POST /connectors/:id/sync — on-demand sync (422 for Slack/disabled)
- Frontend: Sync now button, View history expand panel, /connectors/[id]/edit page

outlook-connector (31 tasks):
- OutlookConnectorService — Microsoft Graph API, MSAL OAuth2 refresh token
- Hourly @Cron in SchedulerService; ConnectorRegistry bootstrap from OUTLOOK_* env vars
- Per-connector dedup via outlookMessageId; attachment download to ATTACHMENT_STORAGE_PATH
- Schema: outlookMessageId String? on RfqIntake; "outlook" in RfqSourceType enum
- Frontend: Outlook type + credentials hint in /connectors/new

simplify-codebase (23 tasks):
- Shared utilities: optionalString, buildPaginatedResponse, useAuthUser(), API_BASE_URL/config.ts
- computeGrandTotal replaced by calcQuoteTotals from @auto8/shared
- VALID_PIPELINE_STATUSES moved to packages/shared; ConnectorSyncSummary consolidated to shared
- Gmail syncMessages() extracted (dedup sync/syncLegacy loop); Slack buildNormalizedSlackIntake() extracted
- ConnectorService interface: sync() + testConnector() added
- ConnectorRegistry: controller removed from providers[]; ConnectorRunModule added to Gmail/Slack modules

mvp1-audit-and-fixes (34 tasks):
- Attachment error path now calls aggregateAttachmentContent before re-throw
- rfq_extract double-enqueue guard (skip when attachments present)
- SMTP_SECURE coercion fixed: compare string !== "false"
- Email body total uses item.subtotal (respects per-item discounts)
- Catalogue search case-insensitive (mode: "insensitive"); NaN guard on basePrice
- Stale job recovery: reset running jobs stuck >5min back to pending
- Extracted customer pre-populates quote draft in RFQ detail page

Verification: typecheck ✓, build ✓ (17 routes), 58/58 tests ✓

## Notes

- OUTLOOK_CLIENT_ID / OUTLOOK_CLIENT_SECRET / OUTLOOK_REFRESH_TOKEN must be configured for Outlook ingestion to activate
- Run `prisma migrate deploy` on deployment (2 new migrations)
- OpenSpec: 5 changes archived to openspec/changes/archive/2026-05-28-*/
