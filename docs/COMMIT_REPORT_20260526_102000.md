# Commit Report

## Summary

- Project: /home/beou/IdeaProjects/auto8
- Branch: x51-commit/20260526-102000-slack-rfq-intake
- Mode: single project
- Timestamp: 2026-05-26T10:20:00
- Branch decision: New branch — large multi-area change (schema, migration, API, tests, UI, docs, OpenSpec artifacts)
- Commit title: feat(rfqs): add Slack RFQ intake connector (source-aware intake model)
- Push target: origin/x51-commit/20260526-102000-slack-rfq-intake

## Uncommitted Audit

- Schema/migration: RfqEmail → RfqIntake model with sourceType enum, 7 Slack provenance fields; forward migration added
- API: Slack connector endpoint (POST /api/rfqs/intake-slack) with HMAC-SHA256 request verification; shared createRfqFromIntake(); rawBody support
- Shared contracts: RfqSourceType, SlackRfqIntakeInput, sourceType/sourceLabel on list+detail views, senderEmail now nullable
- Web UI: source badge in queue list cards; Slack provenance panel in RFQ detail view
- Tests: 3 new/updated test cases (email regression, Slack happy-path, Slack rejection); SLACK_SIGNING_SECRET in test setup
- Docs: README updated; .env.example vars added
- OpenSpec: all 10 tasks marked complete; change artifacts (proposal, design, specs, tasks) added

## Files Planned For Commit

- `.env.example`: SLACK_SIGNING_SECRET, SLACK_ALLOWED_WORKSPACE_IDS vars
- `README.md`: Slack connector section, updated setup steps
- `apps/api/.env.example`: same Slack vars
- `apps/api/prisma/schema.prisma`: RfqIntake model, RfqSourceType enum
- `apps/api/prisma/migrations/20260526093000_add_slack_rfq_intake/migration.sql`: forward migration
- `apps/api/prisma/seed.ts`: updated to rfqIntake, adds Slack demo RFQ-1003
- `apps/api/src/bootstrap.ts`: rawBody: true
- `apps/api/src/rfqs/rfqs.controller.ts`: intake-slack endpoint
- `apps/api/src/rfqs/rfqs.service.ts`: full refactor — intakeSlack, verifySlackRequest, createRfqFromIntake
- `apps/api/test/api.e2e.spec.ts`: Slack test helpers and cases
- `apps/api/test/setup.ts`: Slack env vars
- `apps/web/app/page.tsx`: sourceLabel badge
- `apps/web/app/rfqs/[rfqId]/page.tsx`: source badge + Slack provenance panel
- `packages/shared/src/index.ts`: RfqSourceType, SlackRfqIntakeInput, updated views
- `openspec/changes/add-slack-rfq-intake/`: proposal, design, specs, tasks (all 10 checked)

## Affected Flows

- Runtime: POST /api/rfqs/intake-email and POST /api/rfqs/intake-slack both converge at createRfqFromIntake()
- User flow: RFQ queue shows source badge; detail view shows Slack provenance for Slack-origin RFQs
- Dev workflow: new SLACK_SIGNING_SECRET and SLACK_ALLOWED_WORKSPACE_IDS env vars required for Slack endpoint

## Impact And Risks

- Impact: existing email RFQ workflow fully preserved; downstream quote/approval unchanged
- Risk: migration rebuilds Rfq table under SQLite — tested and applied successfully
- Mitigation: migration copies all existing RfqEmail rows to RfqIntake before dropping old table

## Sensitive/Risk Review

- SLACK_SIGNING_SECRET: env var only, not committed to any tracked file
- Migration: data-preserving INSERT...SELECT before DROP
- No secrets in committed files

## Verification

```bash
npm run typecheck  # all workspaces — PASS
npm run db:seed    # seed with Slack RFQ — PASS
npm run test       # 6/6 tests — PASS
npm run build      # shared + api + web — PASS
```

## Commit Comments

Implements OpenSpec change `add-slack-rfq-intake`:

- Replace RfqEmail with RfqIntake (sourceType: email|slack) + forward migration
- Add POST /api/rfqs/intake-slack with HMAC-SHA256 Slack request verification
- Shared createRfqFromIntake() used by both email and Slack paths
- Extend shared contracts with RfqSourceType, SlackRfqIntakeInput, source fields on views
- Web UI: source badge in queue; Slack provenance panel in detail
- Tests: email regression + Slack valid/invalid cases (6 total, all pass)
- All 10 OpenSpec tasks marked complete
