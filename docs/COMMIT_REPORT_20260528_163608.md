# Commit Report

## Summary

- **Project:** /home/beou/IdeaProjects/auto8
- **Branch:** x51-commit/20260528-163608-mvp1-audit-fixes
- **Mode:** Single project
- **Timestamp:** 2026-05-28 16:36:08
- **Branch decision:** New branch — 14 modified files + 1 untracked, spans multiple areas (API, frontend, background jobs, catalogue, LLM)
- **Commit title:** fix(mvp1): audit fixes — job reliability, catalogue, matching, email totals, extracted customer UI
- **Push target:** origin/x51-commit/20260528-163608-mvp1-audit-fixes

## Uncommitted Audit

- **Background jobs:** Double-enqueue prevention for `rfq_extract` when attachments present; stale `running` job recovery
- **Attachments:** Error path now calls `aggregateAttachmentContent` before re-throwing so RFQ doesn't stay blocked
- **Quote email:** SMTP_SECURE env string coercion fixed; email body total now uses `item.subtotal` (respects per-item discounts)
- **Catalogue:** Case-insensitive search (PostgreSQL `mode: "insensitive"`); NaN guard on basePrice; file-present guard on upload; new `UpdateProductDto` for PATCH endpoint
- **Item matching:** Accept action now clears stale `overrideDescription`/`overrideUnitPrice` null
- **Quote workflow:** `createQuoteFromMatches()` — conflict guard (409 if draft exists) + uses extracted customer company/contact
- **Pipeline status:** `updatePipelineStatus("classified")` enqueues `rfq_extract` if none pending; `quote_submitted` added to filter dropdown; stats use `rfqPipelineStatus`
- **Frontend:** Extracted customer panel on RFQ detail page (pre-populates draft form); duplicate `fetchUsers` removed; `alert()` → `setError()` in catalogue page
- **LLM:** `as Function` cast replaced with `eslint-disable` comment
- **Demo seed:** `status: "completed"` → `status: "done"` (valid BackgroundJob status)

## Files Planned For Commit

- `apps/api/prisma/demo-seed.ts`: Fix BackgroundJob status to "done"
- `apps/api/src/attachments/attachment.service.ts`: Aggregate in error catch path
- `apps/api/src/catalogue/catalogue.controller.ts`: File guard + UpdateProductDto
- `apps/api/src/catalogue/catalogue.service.ts`: Case-insensitive search + NaN guard
- `apps/api/src/catalogue/dto/update-product.dto.ts`: NEW — validated PATCH DTO
- `apps/api/src/jobs/jobs.service.ts`: Stale job recovery (running > 5min → pending)
- `apps/api/src/llm/llm.service.ts`: eslint-disable for unavoidable dynamic call
- `apps/api/src/matching/item-matching.service.ts`: Accept clears override fields
- `apps/api/src/quote-email/quote-email.service.ts`: SMTP_SECURE coercion + email total from subtotal
- `apps/api/src/rfqs/quote-workflow.service.ts`: createQuoteFromMatches conflict guard + extracted customer
- `apps/api/src/rfqs/rfq-intake.service.ts`: Double-enqueue prevention + reclassify triggers rfq_extract
- `apps/web/app/catalogue/page.tsx`: alert → setError
- `apps/web/app/page.tsx`: Stats from rfqPipelineStatus + quote_submitted filter
- `apps/web/app/rfqs/[rfqId]/page.tsx`: Extracted customer panel + pre-populate draft
- `apps/web/lib/api.ts`: Remove duplicate fetchUsers

## Detailed Changes

- `rfq-intake.service.ts`: `rfq_extract` job only enqueued when no attachments; `updatePipelineStatus("classified")` triggers rfq_extract if no pending job exists
- `attachment.service.ts`: catch block now calls `aggregateAttachmentContent(rfqIntakeId)` before re-throwing, ensuring settled aggregate runs even on parse failure
- `jobs.service.ts`: Added stale job recovery — `processPendingJobs()` resets `running` jobs with `updatedAt < now - 5min` back to `pending` at the start of each cron tick
- `quote-email.service.ts`: `SMTP_SECURE` read as string with `!== "false"` comparison; `generateBody()` total uses `item.subtotal` sum
- `catalogue.service.ts`: Prisma `contains` queries now use `mode: "insensitive"` for PostgreSQL; `Number(row.basePrice)` guarded against NaN
- `catalogue.controller.ts`: Guard `if (!file) throw BadRequestException`; `update()` typed as `UpdateProductDto`
- `catalogue/dto/update-product.dto.ts`: New file with `@IsOptional` validators for productName, description, brand, unit, basePrice, isActive
- `item-matching.service.ts`: `updateMatch(accept)` sets `overrideDescription: null, overrideUnitPrice: null` to clear stale overrides
- `quote-workflow.service.ts`: `createQuoteFromMatches` throws 409 if draft already exists; uses `extractedCustomer.customerContact`/`customerCompany` if available
- `rfqs/[rfqId]/page.tsx`: `getExtractedCustomer()` called in `Promise.all`; `extractedCustomer` state; displayed as info panel; passed to `buildDraft()`

## Affected Flows

- **RFQ ingestion with attachments:** No longer double-fires `rfq_extract`; blocked RFQs on attachment parse failure now unblock
- **Background job processing:** Stale running jobs auto-recover on next cron tick
- **Quote email send:** SMTP TLS disabled for dev (`SMTP_SECURE=false`); email line item totals reflect discounts
- **Catalogue upload/search:** PostgreSQL search is now case-insensitive; bad basePrice values don't corrupt DB
- **Match review:** Accepting a match no longer leaves stale override text/price
- **Create quote from matches:** Conflict-safe; pre-populates customer info from LLM-extracted customer data
- **RFQ detail UI:** Extracted customer section visible; draft form pre-fills company/contact

## Impact And Risks

- **Impact:** Multiple reliability and correctness fixes across critical paths; no new features
- **Risk:** Low — all changes are narrowly scoped bug fixes; no schema changes
- **Mitigation:** 58/58 tests pass; typecheck clean; build clean

## Untracked Files

- `apps/api/src/catalogue/dto/update-product.dto.ts`: staged — new DTO for catalogue PATCH endpoint

## Sensitive/Risk Review

- **Secrets:** No secrets in any changed file
- **Schema changes:** None
- **Large/generated files:** None
- **Deleted files:** None

## Verification

```bash
npm run typecheck && npm run build && npm run test
```

Result:

```
typecheck: PASS (0 errors in all 3 workspaces)
build: PASS (all workspaces compile, 15 Next.js routes)
test: 58/58 PASS
```

## Commit Comments

fix(mvp1): audit fixes — job reliability, catalogue, matching, email totals, extracted customer UI

- Prevent double rfq_extract enqueue when attachments present (rfq-intake.service.ts)
- Aggregate attachment content in error catch path so RFQ doesn't stay blocked
- Stale job recovery: running jobs >5min reset to pending on each cron tick
- Fix SMTP_SECURE env coercion (string "false" was truthy, forced TLS in dev)
- Email body total now uses item.subtotal (respects per-item discounts)
- Catalogue search case-insensitive on PostgreSQL (mode: insensitive)
- NaN guard on basePrice in CSV/XLSX upload
- File-present guard on catalogue upload endpoint
- New UpdateProductDto for PATCH /catalogue/:id
- Accept match action clears stale override fields (overrideDescription/Price → null)
- createQuoteFromMatches: 409 conflict guard + extracted customer pre-population
- Stats and filter dropdown now use rfqPipelineStatus (consistent with filter)
- quote_submitted added to pipeline filter dropdown
- Extracted customer panel in RFQ detail page; buildDraft pre-populates company/contact
- Remove duplicate fetchUsers function; alert() → setError() in catalogue page
- Replace `as Function` cast with eslint-disable comment in LlmService
- Demo seed: BackgroundJob status "completed" → "done" (valid status)

Verification: typecheck ✓, build ✓, 58/58 tests ✓

## Notes

- No remaining risk. All changes are bug fixes from mvp1-audit-and-fixes OpenSpec change (53/53 tasks complete).
