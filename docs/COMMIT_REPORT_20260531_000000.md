# Commit Report

## Summary

- Project: /home/beou/IdeaProjects/auto8
- Branch: x51-commit/20260531-000000-billing-toggle-plugin-arch
- Mode: single project
- Timestamp: 2026-05-31 00:00:00
- Branch decision: New branch — large multi-area changes spanning 3 distinct features (billing-toggle, pluggable-module-architecture, plugin-architecture-hardening), new untracked files, architecture changes
- Commit title: feat: billing toggle, pluggable module architecture, plugin hardening
- Push target: origin/x51-commit/20260531-000000-billing-toggle-plugin-arch

## Uncommitted Audit

- **Billing toggle**: BillingGuard/Service/Controller updated with `BILLING_ENABLED` env var check; frontend billing page shows disabled state; `.env.example` + ARCHITECTURE.md updated
- **Pluggable module architecture**: New `apps/api/src/plugin-registry/` module with `PluginRegistryModule`, `PluginRegistryService`, plugin interfaces; 9 plugin manifest files created (6 connector + 3 non-connector); `ConnectorRegistryModule/Service` migrated to use registry; `AppModule` switched to `PluginRegistryModule.register([...])` composition; `JobType` → open string; contract checks expanded (53 checks)
- **Plugin architecture hardening**: `ConnectorPlugin.syncable: boolean` added; discriminated union `ConnectorPluginManifest | ModulePluginManifest`; `WEBHOOK_EMITTER_TOKEN` injection token replaces manual property injection; `AppModule.onModuleInit()` removed; `validate(moduleRef)` enforcement; additional contract checks (71 checks total); tests updated (61 passing)

## Files Planned For Commit

- `.env.example`: BILLING_ENABLED and new env vars added
- `apps/api/src/app.module.ts`: PluginRegistryModule.register() composition, removed onModuleInit()
- `apps/api/src/billing/billing.controller.ts`: checkEnabled() guard on mutation endpoints
- `apps/api/src/billing/billing.guard.ts`: BILLING_ENABLED early-return + startup warning
- `apps/api/src/billing/billing.service.ts`: disabled synthetic subscription response
- `apps/api/src/connector-registry/connector-registry.module.ts`: removed all 6 connector module imports
- `apps/api/src/connector-registry/connector-registry.service.ts`: uses PluginRegistryService, syncNow() uses plugin.syncable
- `apps/api/src/connector-registry/connector-registry.service.spec.ts`: updated for new constructor args
- `apps/api/src/jobs/jobs.service.ts`: JobType → open string
- `apps/api/src/quote-email/quote-email.module.ts`: added WebhooksModule import
- `apps/api/src/quote-email/quote-email.service.ts`: @Inject(WEBHOOK_EMITTER_TOKEN) constructor injection
- `apps/api/src/rfqs/quote-workflow.service.ts`: @Inject(WEBHOOK_EMITTER_TOKEN) constructor injection
- `apps/api/src/rfqs/rfq-intake.service.ts`: @Inject(WEBHOOK_EMITTER_TOKEN) constructor injection
- `apps/api/src/rfqs/rfqs.module.ts`: added WebhooksModule import
- `apps/api/src/webhooks/webhook-emitter.service.ts`: exported WEBHOOK_EMITTER_TOKEN
- `apps/api/src/webhooks/webhooks.module.ts`: provides + exports WEBHOOK_EMITTER_TOKEN
- `apps/api/src/workspace/workspace-isolation.spec.ts`: updated for new ConnectorRegistryService constructor
- `apps/web/app/billing/page.tsx`: disabled state UI
- `docs/ARCHITECTURE.md`: BillingGuard + BillingModule toggle description
- `tools/contract-checks/index.ts`: 71 checks total (syncable, WEBHOOK_EMITTER_TOKEN, no-hardcoded-push-only, plugin manifest)
- **NEW**: `apps/api/src/plugin-registry/` (5 files): index.ts, plugin.interfaces.ts, plugin-registry.module.ts, plugin-registry.service.ts, plugin-registry.service.spec.ts
- **NEW**: 9 plugin manifests: gmail.plugin.ts, slack.plugin.ts, outlook.plugin.ts, whatsapp.plugin.ts, telegram.plugin.ts, zalo.plugin.ts, rfqs.plugin.ts, quotes.plugin.ts, webhooks.plugin.ts
- **NEW**: `docs/PLUGIN_ARCHITECTURE.md`

## Detailed Changes

- `billing.guard.ts`: reads `process.env['BILLING_ENABLED']`; if not `'true'`, logs warning and returns true immediately (no enforcement)
- `billing.service.ts`: `SubscriptionView.enabled: boolean` added; returns `{ enabled: false, plan: null, status: 'disabled' }` when billing disabled
- `billing.controller.ts`: `checkEnabled()` throws `ServiceUnavailableException` on mutation endpoints when billing disabled
- `plugin.interfaces.ts`: `ConnectorPlugin { syncable: boolean }` + discriminated union `ConnectorPluginManifest | ModulePluginManifest`
- `plugin-registry.service.ts`: `PluginRegistryService` holds maps for connector plugins, webhook events, manifests; `validate(moduleRef)` checks service token resolution at startup
- `plugin-registry.module.ts`: `@Global()` DynamicModule; static `register(manifests)` method; calls `validate()` in `onModuleInit()`
- `connector-registry.service.ts`: `resolveConnectorService(type)` via `pluginRegistry.getConnectorPlugin(type)` → `moduleRef.get(serviceToken)`; `syncNow()` uses `plugin.syncable` for push-only detection
- `connector-registry.module.ts`: removed all 6 optional connector service fields and `ModuleRef`-based wiring
- `webhook-emitter.service.ts`: exports `WEBHOOK_EMITTER_TOKEN = 'WEBHOOK_EMITTER_TOKEN'`
- `rfq-intake.service.ts`, `quote-workflow.service.ts`, `quote-email.service.ts`: replaced `webhookEmitter?` property assignment with `@Inject(WEBHOOK_EMITTER_TOKEN)` constructor injection
- `app.module.ts`: removed `onModuleInit()` entirely; uses `PluginRegistryModule.register()`

## Change Content Summary

- **Billing toggle**: Deploy-time `BILLING_ENABLED=true` flag; guard short-circuits when disabled; mutation endpoints 503; frontend shows informative disabled state
- **Plugin architecture**: Each NestJS feature module now self-declares capabilities via `PluginManifest`; connector routing is data-driven via registry; no central hardcoded type lists; `AppModule` is a composition root
- **Plugin hardening**: Push-only detection via `syncable` field; webhook emitter uses proper DI token; `AppModule.onModuleInit()` eliminated; startup validation warns on misconfigured plugins
- **Tests**: 61 unit tests across plugin-registry, connector-registry, portal, workspace-isolation

## Affected Flows

- **Runtime flow**: BillingGuard allows all requests when BILLING_ENABLED != true; billing endpoints 503 on mutations; connector sync routes via plugin registry; webhook emitter injected cleanly at startup
- **User/API flow**: GET /billing/subscription returns `{ enabled: false }` when billing off; new connector types can be added by creating a single plugin manifest file
- **Background/deployment/dev workflow**: BILLING_ENABLED env var controls billing enforcement; new `docs/PLUGIN_ARCHITECTURE.md` explains extension pattern; `npm run verify:contracts` validates plugin registry integrity

## Impact And Risks

- **Impact**: High — architecture change to connector dispatch and webhook emitter DI; billing guard changes all authenticated routes
- **Risk**: Medium — all changes verified by typecheck + build + 61 unit tests + 71 contract checks; pre-existing test failures (rfq-classification, rfq-extraction, e2e) are unrelated
- **Mitigation**: All connector types still work through the plugin registry; backward-compatible API surface

## Untracked Files

- `apps/api/src/plugin-registry/` (5 files): staged — new core infrastructure
- 9 `*.plugin.ts` files: staged — new plugin manifests
- `docs/PLUGIN_ARCHITECTURE.md`: staged — new documentation

## Sensitive/Risk Review

- Secrets checked: No secrets in changed files; `.env.example` only has placeholder values
- Large/generated files: None
- Deleted files: None (imports removed from modules, not files deleted)

## Verification

```bash
npm run typecheck
npm run build
npm run verify:contracts
npm run test
```

Result:

```text
typecheck: clean (api + web)
build: success (33 routes)
verify:contracts: 71/71 PASS (was 53, added 18 for plugin hardening)
tests: 61/61 unit tests pass; pre-existing failures in rfq-classification (4), rfq-extraction (1), e2e (unrelated)
```

## Commit Comments

feat: billing toggle + pluggable module architecture + plugin hardening

Changes:
- billing-toggle: BILLING_ENABLED env var controls BillingGuard enforcement; mutation endpoints return 503 when disabled; frontend billing page shows disabled state
- pluggable-module-architecture: PluginRegistryModule with DynamicModule.register() pattern; 9 plugin manifests (6 connector, 3 non-connector); ConnectorRegistryService/Module migrated to use PluginRegistryService; AppModule becomes composition root; JobType → open string
- plugin-architecture-hardening: ConnectorPlugin.syncable:boolean replaces hardcoded push-only list; WEBHOOK_EMITTER_TOKEN DI injection replaces AppModule.onModuleInit() property assignment; validate(moduleRef) warns on misconfigured plugins; discriminated union ConnectorPluginManifest|ModulePluginManifest
- verify:contracts: 71 checks (was 53)
- docs/PLUGIN_ARCHITECTURE.md: extension guide for new connector and job-handler plugins

Verification: typecheck ✓, build ✓ (33 routes), verify:contracts 71/71 ✓, 61 unit tests ✓

## Notes

- Pre-existing test failures (rfq-classification, rfq-extraction, e2e) not introduced by these changes
- openspec/changes/ artifacts for billing-toggle, pluggable-module-architecture, plugin-architecture-hardening not included (in prior commit on base branch)
